# tree format
group A:
    query1:
        name: first
        query: SELECT * FROM users;
    query2:
        name: second
        query: SELECT * FROM departments;
group B:
    query1:
        name: third
        query: SELECT * FROM users;

